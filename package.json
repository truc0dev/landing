{
  "name": "landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd landing-angular && npm install && npm run build",
    "start": "cd landing-angular && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
