{
  "buildCommand": "cd landing-angular && npm install && npm run build",
  "outputDirectory": "landing-angular/dist/landbow/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
